En este ejercico se pide:

  - En las rutas 'public' y 'public/child', se debe controlar mediante el guard correspondiente, del servicio 'auth.service', el acceso a la ruta sólo si tiene la variable access a true.

  - En la ruta 'lazy', contolar la carga del módulo para que solo cargue si tiene la variable auth a true.

  - Añadir al objeto data, de la ruta 'public', los datos obtenidos mediante el método resolve del 'auth.servie'.

  - Se debe ver en pantalla el json con el resultado del data en la ruta 'public'.